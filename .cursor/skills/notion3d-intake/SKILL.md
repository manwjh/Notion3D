---
name: notion3d-intake
description: >-
  Legacy — superseded by render-first in notion3d-pipeline. Clarify only when
  render would clearly fail (e.g. missing critical dimension for a print fit).
---

# Intake（已并入 render-first）

**默认直接 render**，用合理 mm 假设。仅当缺 **一个** 无法假设的关键尺寸时再问。

不要因 task_class C / 卡通 / 人像而拒绝 render — 先尝试简模，再向用户说明局限。

→ [notion3d-pipeline](../notion3d-pipeline/SKILL.md)
