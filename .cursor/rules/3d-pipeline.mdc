---
description: Notion3D 标准前后端 + MCP Agent 集成约定
globs:
  - apps/**/*
  - data/**/*
  - docs/**/*
---

# Notion3D 项目约定

- 产品定位：**OpenSCAD 建模工作台**（描述 → 建模 → 预览 → 导出 SCAD/STL）
- 标准架构：`apps/web`（**Vue 3**）+ `apps/api` + `apps/mcp-server`
- **Agent 集成**：MCP Server（`notion3d-mcp`）+ Skill + [AGENTS.md](../../AGENTS.md)
- Agent **必须**使用 `notion3d_*` MCP tools，禁止 curl 绕过 MCP
- 产物：`data/projects/{id}/versions/`
- 调试：`make api` → `make web`；Web http://localhost:5173
- Agent 调试：API 运行后配置 MCP（`.cursor/mcp.json`），可选 `make mcp`
- SCAD 渲染：`POST /api/projects/{id}/render-scad`
- **智能建模由 Agent 负责**（MCP + `notion3d_render_scad`）；Web chat 仅规则模板，不配置 LLM Key
- 打印/切片 API 保留但 UI 暂不暴露
