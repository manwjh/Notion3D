# Legacy OpenSCAD Templates

OpenSCAD 模板已迁移至 `templates/legacy/scad/builtin/`。

新模板请使用 ForgeCAD（`templates/builtin/*/model.forge.js`）。

Legacy API：`POST /api/projects/{id}/render-scad` 仍可用于旧 SCAD 模板。
