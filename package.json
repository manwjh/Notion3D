{
  "name": "3d-pipeline",
  "private": true,
  "scripts": {
    "dev:api": "cd backend && python3 -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:web": "cd frontend && npm run dev",
    "install:api": "cd backend && python3 -m pip install -r requirements.txt",
    "install:web": "cd frontend && npm install",
    "install:all": "npm run install:api && npm run install:web"
  }
}
